## 1. Skill — `ssc-ads-brief` appends instead of refusing

- [ ] 1.1 In `plugins/ssc-content/skills/ssc-ads-brief/SKILL.md`, replace **Step 2 (produce-once guard)** with a **taken-set read**: `list_briefs(idea)` still runs first, but its result is now *input*, not a stop condition. Hold every returned brief (any status — draft and approved alike): its `angle_label` **and** its five narrative fields. Delete the STOP branch and its "discard all briefs in the dashboard first" message entirely. Rename the step to reflect what it now does (e.g. "Read the existing angle set — the taken set").
- [ ] 1.2 Extend **Step 3 (angle selection)** so its distinctiveness rule runs **across the taken set**, not just within the batch: each proposed angle anchors a persona trigger point / objection / myth that no existing brief already anchors, and that no other candidate in the batch anchors. State that a candidate carrying a different `angle_label` but the same anchor and the same argument as an existing brief **is** a duplicate.
- [ ] 1.3 Extend the **never-pad** rule to the append case, and promote "wrote nothing" from a degenerate case to an **ordinary outcome**: an idea whose distinct angles are exhausted yields **no** brief, reported plainly (name the reason; suggest sharpening the concept if more angles are genuinely wanted). Never relax the distinctiveness bar to produce a non-empty result.
- [ ] 1.4 Extend **Step 5 (quality gate)** so the **distinctiveness** criterion is scored against the taken set **plus** the current batch — a near-duplicate of an existing brief caps low and is dropped/regenerated by the loop that already exists. No angle rated ≤3 is ever saved (unchanged).
- [ ] 1.5 Update **Step 6 / Step 7** (save + summary): saving is unchanged (`save_brief` mints DRAFTs, one row per angle), but the summary now reports the append explicitly — how many briefs the idea already carried, how many angles were appended, and the fact that no existing brief was touched. Remove the summary's "To regenerate the set, discard all briefs there first" line.
- [ ] 1.6 State the **read-only-existing-briefs** invariant in Governance: the skill never deletes, edits, re-writes, re-scores, re-labels, or re-statuses an existing brief; appending via `save_brief` is its only write to the angle set. Confirm `delete` and `edit` do **not** appear in the frontmatter `tools:` list (today it is `[get_idea, get_channel_plan, get_knowledge, list_briefs, save_brief]` — it must stay that way; the invariant is structural, not merely instructed).
- [ ] 1.7 Replace the **deletion prose**. The "discard ALL briefs to regenerate" remediation is deleted outright (1.1, 1.5). What replaces it is a statement of what discarding an angle **costs**, describing the server's **actual current** semantics at ship time: the brief is HARD-deleted (the `briefs` table has no tombstone — the angle is gone for good), and today the server additionally **refuses** while the brief is `approved` (`brief_approved`) or still has creatives (`brief_has_creatives`), and **unbinds** rather than deletes its content (`content.brief_id → NULL`). Do not describe the cascade as if it had shipped. Do not recommend deletion.
- [ ] 1.8 Rewrite the frontmatter `description` and the file's opening paragraphs: drop every "produce-once" / "produces the angle set exactly once" / "never appends" statement (they appear in the description, the intro, Step 2, Output, and Governance — sweep all of them) and replace with the append semantics.

## 2. Command — `/ssc.ads-brief`

- [ ] 2.1 In `plugins/ssc-content/commands/ssc.ads-brief.md`, update the `description` frontmatter and the "What to do" section: the dispatched skill **appends** distinct new angles on every invocation; it no longer runs a produce-once guard and no longer stops when briefs exist.
- [ ] 2.2 Rewrite "After it runs": remove *"This command produces the angle set once: if briefs already exist it stops… (discard all there first to regenerate…)"*. Replace with: re-invoke any time to propose further distinct angles; existing briefs (and the copy and creatives anchored to them) are never touched; if the concept supports no further distinct angle the skill says so and writes nothing.

## 3. Consistency sweep

- [ ] 3.1 Grep the plugin for stale references to the produce-once behaviour (`produce-once`, `produces once`, `discard all`, `never appends`, `won't append`) and fix every hit — the agent/skill/command prose must not contradict itself. Note: `/ssc.ads-brief` is dispatched **directly** by its command (no agent), so `ssc-ads-agent` should not reference it — confirm rather than assume.
- [ ] 3.2 Confirm the change touches **no other skill**: `ssc-ads-writer` still takes ONE `brief_id`, `ssc-image` still anchors on ONE `brief_id` and keeps its copy-gate rule (brief scope normal; single-brief idea-scope fallback, announced; multi-brief-no-lineage STOP). Nothing downstream changes shape when the angle set grows.
- [ ] 3.3 Confirm every tool the revised skill references still exists on the live BrandOS surface with the argument names used (`list_briefs(idea)`, `save_brief(idea_id, channel, angle_label, …)`) — referencing a renamed/removed server tool is a recurring shipped-bug class in this repo.

## 4. Verification

- [ ] 4.1 Verify against the delta spec's **plugin** requirements (`openspec/changes/ads-angle-set-curation/specs/ads-angle-set-curation/spec.md`): always-appends; taken set includes drafts; distinctiveness across the set and within the batch; never pads (empty result is ordinary); existing briefs read-only and no `delete`/`edit` tool; deletion prose replaced by its true cost. Each has corresponding prose in the skill.
- [ ] 4.2 Confirm the governance invariants survive: propose-only (`save_brief` mints DRAFTs, no `approve`, no gate flip, no `status` write), single BrandOS MCP surface, Vietnamese persisted prose (`angle_label`, the five narrative fields, `comment`), ad channel only, one concept per invocation.
- [ ] 4.3 Live check on an idea that already carries briefs (e.g. `BGerzuw4JrrSz3Qd`, five approved labelled briefs): run `/ssc.ads-brief <idea_id>`, then `list_briefs` before/after. Confirm (a) the skill did not stop, (b) every pre-existing brief row is unchanged (id, `angle_label`, narrative fields, `score`, `comment`, `status`), (c) any appended row is a `draft` with an anchor no existing brief holds, and (d) if no distinct angle remained, the skill wrote nothing and said so — rather than saving a near-duplicate.

## 5. Server handoff (NOT this repo — BrandOS MCP server)

- [ ] 5.1 Hand the delta spec's **server** requirements to the BrandOS team as the contract for `delete(entity='brief')`: keep the `brief_approved` refusal; REPLACE the `brief_has_creatives` refusal with a cascade (creatives hard-deleted + media purged; content rows **hard-deleted**, replacing `content.brief_id ON DELETE SET NULL`); one atomic, individually-audited transaction.
- [ ] 5.2 Hand over the capability-escalation rule: no dependents → `edit`; **any** dependents → **`approve`** (an `edit`-only caller is refused `forbidden`, writing nothing). This is what makes a propose-only agent structurally unable to destroy produced work.
- [ ] 5.3 Hand over the live-schedule refusal (any content row referenced by a live schedule entry ⇒ the whole delete refuses atomically) and the preflight requirement (report counts of creatives / images purged / content rows **before** destroying; `dry_run` flag vs. separate preflight read is the server's call).
- [ ] 5.4 Once the server cascade ships, update `ssc-ads-brief`'s "what discarding an angle costs" paragraph (task 1.7) to describe the cascade instead of today's refusals. The prose follows the server; it never leads it.

## 6. Open questions (not resolved by this change — see `design.md`)

- [ ] 6.1 **`save_post_content`'s ad-content inference rule** (tracked on `ads-writer-brief-lineage`). With `ON DELETE SET NULL` replaced by a cascade, a NULL `brief_id` becomes rare — making the server's inference of an omitted `brief_id` even more indefensible. **Recommend the server REJECT an omitted `brief_id` on ad content rather than guess one.** Server decision.
- [ ] 6.2 **N parallel visual chains per brief.** The live `delete`/`edit` tool descriptions say a creative belongs to "one of a brief's N **PARALLEL** visual chains" and that a layer "may legitimately carry SEVERAL approved creatives at once (one per chain)", with `set_cover` picking the hero composite — while `/ssc.image` assumes ONE linear chain per brief. Live data can neither confirm nor refute this (no brief yet has an approved creative), and the server's tool docs have already proven wrong once today. **Settle this before it bites:** if chains really are parallel, `/ssc.image`'s state machine advances on the wrong chain.
- [ ] 6.3 **Do existing orphaned / mis-stamped ad content rows need a server audit?** (tracked on `ads-writer-brief-lineage`.) The cascade prevents new orphans; it heals no existing row. The plugin cannot answer this — it has no record of which angle each historical row was written from.
