## 1. Rewrite the skill

- [x] 1.1 Rewrite `plugins/ssc-content/skills/ssc-ads-brief/SKILL.md` end-to-end to the produce-once, 4-5 rated angle-brief model. Frontmatter: keep `name`/`metadata.section`/`capability`; rewrite `description`; set `metadata.tools: [get_idea, get_channel_plan, list_post_content, get_knowledge, list_briefs, save_brief]` (remove `update_idea`; add `list_briefs` + `save_brief`). Body procedure: (a) resolve ONE approved concept via `get_idea` (gate `status='approved'` + `channel='ad'`; resolve persona detail-doc path; drop the now-unneeded `idea.version` hold); (b) resolve ad-set `build_spec` via `get_channel_plan` for each angle's `why_now`; (c) load persona detail doc via `get_knowledge` as the primary angle source; (d) copy-approval gate via `list_post_content` (STOP if no approved `copy`); (e) produce-once guard via `list_briefs` (STOP if ≥1 brief exists, route operator to dashboard, write nothing); (f) select up to five angles each anchored to a DISTINCT persona trigger/objection/myth realized through an approved copy, never padding; (g) per angle derive the five narrative fields (`story_moment` = `Không áp dụng — concept không thuộc dạng kể chuyện.` when not story-led) plus a mandatory distinct Vietnamese `angle_label` plus a 1-5 `score` + one-line Vietnamese `comment`; (h) quality gate — drop + regenerate any ≤3 until all ≥4 on distinctiveness/grounding/strategic-sharpness/authenticity (no compliance scan); (i) save each passing angle as a DRAFT brief via `save_brief(idea_id, channel='ad', angle_label, hook_direction, core_message, why_now, story_moment, cta, score, comment)` then STOP. Governance/Output sections: draft-only propose-only (never `approve` any entity incl. `brief`, never `edit`-demote, never `update_idea` for narrative fields, never publish/schedule, never flip a gate); never derive/pass `theme`; all persisted prose Vietnamese; document the server "Change 2" dependency (angle_label nulled + one-brief-per-idea today, payoff inert until Change 2, `angle_label` set anyway = forward-compatible).

## 2. Fix downstream stale references

- [x] 2.1 Update `plugins/ssc-content/commands/ssc.ads-produce.md` — rewrite the `description` frontmatter and the two brief-path body paragraphs (~L38, ~L44) from the retired "revises the idea's five creative-brief fields … no draft/approve step, direct overwrite each run via `update_idea`" model to "produces 4-5 rated DRAFT angle briefs via `save_brief`; operator approves one in the dashboard". Keep the router logic (`section=='creative_brief'` → `ssc-ads-brief`) and the propose-only language; only the write-mechanics description changes. Remove every `update_idea` mention tied to the brief path.
- [x] 2.2 Update `plugins/ssc-content/skills/ssc-ads-writer/SKILL.md` — find the sibling mention of `ssc-ads-brief` and correct any description of its mechanics that still implies the single-`update_idea`-brief model; align it to the multi-angle draft-brief model. Change nothing about the writer's own behavior.
- [x] 2.3 Update root `CLAUDE.md` — the pipelines table row / one-liner describing `ssc-ads-brief` as revising "the idea's creative-brief fields … direct overwrite each run via `update_idea`" → the multi-angle draft-brief model (`save_brief` drafts, operator approves).

## 3. Cross-file consistency check

- [x] 3.1 Verify the change is internally consistent: no `update_idea` reference remains tied to the ads-brief/creative-brief path in any command/agent/skill; every MCP tool the rewritten `ssc-ads-brief` references exists on the live BrandOS surface (`get_idea`, `get_channel_plan`, `list_post_content`, `get_knowledge`, `list_briefs`, `save_brief`); the skill's frontmatter `tools:` matches its body's tool calls; and the propose-only invariant holds (no `approve` / `edit`-demote / publish / gate-flip anywhere in the skill). Report any residual drift.

## 4. Brief-first evolution (supersedes the copy-first order in §1–§3)

Per `docs/superpowers/specs/2026-07-12-ads-brief-first-production-design.md`, the ads-produce pipeline inverts to **brief-first**: the brief is produced and chosen BEFORE copy, and copy is written FROM it. The §1 rewrite lands, then:

- [x] 4.1 `ssc-ads-brief` drops the copy-approval gate and `list_post_content`; angles derive from the concept (`title`/`tags`/`ad_notes`) + `build_spec` + persona doc. Frontmatter `tools:` → `[get_idea, get_channel_plan, get_knowledge, list_briefs, save_brief]` (removes `list_post_content`).
- [x] 4.2 New command `plugins/ssc-content/commands/ssc.ads-brief.md` (`<ideaId>` [, `date`]) dispatches `ssc-ads-brief` as the pipeline's FIRST step.
- [x] 4.3 `ssc-ads-writer` requires `brief_id`, reads the one approved brief via `list_briefs(idea_id)` + `id` filter (there is no `get_brief` tool), STOPs unless it is an approved brief, and grounds `copy` in ONLY that brief; adds `list_briefs` to `tools:`; drops the `date` selector.
- [x] 4.4 `/ssc.ads-produce` takes `<ideaId> <briefId> [section]` and dispatches `ssc-ads-writer` only — the `creative_brief` router is removed.
- [x] 4.5 Root `CLAUDE.md` pipelines table reflects brief-first (`/ssc.ads-brief` first, then `/ssc.ads-produce <ideaId> <briefId> [section]`).
