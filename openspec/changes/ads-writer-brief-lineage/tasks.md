## 1. Skill change

- [x] 1.1 In `plugins/ssc-content/skills/ssc-ads-writer/SKILL.md`, add `brief_id` to the `save_post_content` call so every saved ad content row records the angle brief it was written from. The value is the `brief_id` the skill already requires as an input and resolved via `list_briefs` — never inferred. Update the save step's prose, the frontmatter `description`'s mention of the save call, and any other place that enumerates the `save_post_content` arguments, so no statement in the file still describes a save without `brief_id`. **DONE** — shipped: `brief_id` is passed on every save, in all four sections (Step 8; frontmatter description; Output; Governance).
- [x] 1.2 State in the skill why the argument exists: omitting `brief_id` does **not** null the field — for `ad` content the server binds one **by INFERENCE**, picking one of the idea's several approved briefs, so an omitted argument yields a **stamped angle the skill did not choose**, not an empty one. An explicit value always wins. A future editor who removes the argument as "redundant" sees no change in the data shape (the field stays populated) and silently reintroduces wrong-angle grounding. **DONE** — shipped: the Step 8 "why" note now states the inference hazard, including that a wrong stamp is silent and undetectable downstream.

## 2. Consistency

- [x] 2.1 Confirm `save_post_content`'s live schema still accepts `brief_id` on any channel with the documented "an explicit value always wins" semantics, and that the argument name is exactly `brief_id`. **DONE** — confirmed; the explicit value is what overrides the server's inference for `ad` content.
- [x] 2.2 Check the sibling text producer `ssc-post-produce` — confirm rather than assume, and note the finding. **DONE / finding:** `ssc-post-produce` calls **no** write tool at all — it drafts in-conversation and hands the set to `ssc-post-authority`, which is what calls `save_post_content` for `post` content, without a `brief_id`. The server binds `brief_id` for `post` content correctly (the idea's single brief is resolved server-side), so **no change is needed on the post path**.

## 3. Verification

- [x] 3.1 Verify against the delta spec: every requirement and scenario in `openspec/changes/ads-writer-brief-lineage/specs/ads-copy-brief-lineage/spec.md` has corresponding prose in the skill. **DONE** — the skill covers: `brief_id` on every save in all four sections; never derived/inferred by the skill; the inference hazard and why the argument must not be removed; and the downstream `/ssc.image` brief-scoped resolution.
- [x] 3.2 Confirm the change preserves `ssc-ads-writer`'s governance invariants: propose-only (saves DRAFT rows, never approves, never edits or deletes a row, never flips a gate), single MCP surface, Vietnamese persisted prose. Adding an argument must not touch any of them. **DONE** — all intact.
- [x] 3.3 Live check. **DONE — performed 2026-07-14 against live server data, and it refuted this change's original premise.** Ad content rows do **not** carry a null `brief_id` when the writer omits it: idea `BGerzuw4JrrSz3Qd` carries **five** approved, labelled angle briefs (server "Change 2" has shipped), and **all 20** of its ad content rows came back stamped `brief_id: "IeZb6HjExf2PtUJD"` — written by a version of `ssc-ads-writer` that never passed one. The server binds the field **by inference**. The fix is unchanged (pass `brief_id` explicitly — an explicit value always wins) but the hazard it prevents is a **silently wrong angle stamp**, not a missing one. See the Drift Log in `design.md`.

## 4. Follow-up (not this change)

- [ ] 4.1 **Server question, tracked in `design.md`'s Open Questions:** the server's inference rule for `ad` content is unknown and unaudited — which of an idea's briefs does it pick, and on what basis? Existing rows may carry a wrong angle that no consumer can detect. Decide whether an audit or a re-stamp of pre-change ad content rows is needed. The plugin cannot answer this: it has no record of which angle each historical row was written from.
