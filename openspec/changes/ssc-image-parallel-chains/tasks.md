## 1. Chain model in the skill

- [ ] 1.1 Add a **chain-resolution step** to `plugins/ssc-content/skills/ssc-image/SKILL.md`, before the state machine: from `list_creatives(brief_id)`, group creatives into chains by the lineage already on every row — each **approved `background`** is a chain ROOT (chain id = that creative's id); a `model` belongs to the chain of its `background_creative_id`; a `composite` belongs to the chain of its `scene_creative_id`'s chain; `product` rows are brief-level and belong to no chain. Document that an unresolvable lineage parent makes the row **unassignable** → report and STOP, never default it into a chain.
- [ ] 1.2 Document the three creative statuses (`draft` / `approved` / `discarded`) and define `approved(L)` and `has_drafts(L)` **inside a chain**: a `discarded` row is ignored entirely — it is not a pending draft and never blocks a fresh candidate.
- [ ] 1.3 Add the **chain-selection rule**: no approved background → active layer is `background` (chain creation); exactly one approved background and no `chain` → that chain, resolved without asking; more than one approved background and no `chain` → **STOP and ask**, listing each chain (root background creative id + one-line gist of its `generation_prompt` + how far it has got); an explicit `chain` that is not an approved background of the brief → STOP naming the valid roots. Never pick a chain silently.

## 2. Chain-scoped state machine and layer procedures

- [ ] 2.1 Rewrite the Step 6 state machine so **every** predicate is evaluated inside the SELECTED chain (not across the brief), and so exactly one layer of exactly one chain runs per invocation. Replace the old rule-11 terminal: a **chain** is complete when it has an approved composite; the **brief** is never "complete".
- [ ] 2.2 Rewrite Step 7a (`background`): keep 3 distinct readings, and state that this layer is **chain CREATION** — it runs when the brief has no approved background, or when the operator explicitly asks for an additional chain via `new-chain` (or `layer: background`). Without that opt-in, a brief with an approved background does NOT re-enter this layer (no credit-burning on unwanted new roots).
- [ ] 2.3 Rewrite Step 7b (`model`): gate on THIS chain's root background being approved, and pass `background_creative_id` = that chain's ROOT (never "an approved background of the brief"). One candidate in flight **per chain**; another chain's pending model draft does not block it.
- [ ] 2.4 Rewrite Step 7c (`product`): brief-level, upload-only, never generated and never uploaded by the skill. No approved product → STOP and ask. Exactly one approved product → use it. **Several approved products → STOP and ask which**, unless the operator supplied `product: <creative_id>`. Never guess.
- [ ] 2.5 Rewrite Step 7d (`composite`): `scene_creative_id` = **the selected chain's** approved `model` creative; `product_creative_id` = the approved brief-level product (single, or the one named by `product`). Never compose one chain's scene with another chain's model. An approved composite completes THAT chain; the skill never calls `set_cover`.

## 3. Revise path, inputs, and output

- [ ] 3.1 Rewrite Step 8 (revise) as **chain-scoped**: the note lands on the active layer of the SELECTED chain and rewrites its prompt, issuing ONE fresh generate call for that chain. Base the rewrite on the selected chain's pending draft `generation_prompt` (per-row, lineage-bearing) — and document that `list_creative_prompts(brief_id)` is **brief-level per layer** (shared across chains), so it must not be used as the base when it belongs to another chain.
- [ ] 3.2 Add the three new optional inputs to the skill's **Inputs** section: `chain: <background_creative_id>`, `new-chain` (or `layer: background`), and `product: <creative_id>` — each with its resolution rule and its STOP.
- [ ] 3.3 Rewrite the Step 11 output block so the summary **names the chain worked** (root background creative id + its one-line gist) and **how many chains the brief now has**, and so the "next action" lines are chain-aware (advance this chain / start another with `new-chain` / pick the hero composite in the dashboard).
- [ ] 3.4 Update the skill's frontmatter `description` and the **Governance** section to state the chain-scoped invariants: state is chain-scoped, one layer of one chain per invocation, chain selection is explicit (STOP-and-ask on ambiguity), a chain completes but a brief never does, and `set_cover` remains the operator's (still absent from `tools:` — the `tools:` list does not change).

## 4. Command entry point

- [ ] 4.1 Update `plugins/ssc-content/commands/ssc.image.md` to accept and pass through `chain`, `new-chain`, and `product` unchanged, alongside the existing `revise` / `model` / `layout_hint` / `hard_paste` / `period`. Keep it a thin entry point that dispatches `ssc-image` and holds no orchestration logic — it must not resolve or choose a chain itself.

## 5. Verification

- [ ] 5.1 Re-read the rewritten skill hunting for **any surviving brief-scoped predicate** — every "approved background", "the approved model", "all four layers approved", or "complete for this brief" must now be chain-scoped or removed.
- [ ] 5.2 Check the rewritten skill and command against `openspec/changes/ssc-image-parallel-chains/specs/ads-image-visual/spec.md` requirement by requirement: every scenario has prose in the skill (or command) that would produce that behaviour. Report any requirement with no implementation evidence.
- [ ] 5.3 Confirm the untouched invariants survived: the three preconditions (approved ad idea → approved brief → approved copy for that brief) and their scope rule, the five prompt rules, the grounding order, propose-only governance, single MCP surface, save-to-server, no baked-in text, ad channel only, Vietnamese operator prose — and that the frontmatter `tools:` list is byte-for-byte unchanged (no `set_cover`, no `upload_product_creative`, no `approve`/`edit`).
- [ ] 5.4 Live end-to-end run on a brief that has **two approved backgrounds**: confirm the skill STOPs and asks which chain; then re-run with `chain: <id>` and confirm the model it generates is conditioned on THAT chain's root, and that the summary names the chain and the brief's chain count.
